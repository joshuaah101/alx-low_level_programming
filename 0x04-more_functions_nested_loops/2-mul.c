#include "main.h"

/**
 * mul - a function that finds the product of two numbers
 * @a: first number
 * @b: second number
 *
 * Return: 0
 */

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
